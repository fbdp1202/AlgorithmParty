#include <bits/stdc++.h>
using namespace std;


int T, N;

int read_int() {
    int x;
    scanf("%d", &x);
    return x;
}

int main () {
    setbuf(stdout, NULL);

    T = read_int();
    for (int t=1; t<=T; t++) {
        printf("Case #%d\n", t);
    }
    return 0;
}